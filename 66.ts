
type Primes = number[];

type Integer = number;

type Fraction = {
    num: Integer,
    den: Integer
}

class Fraction {

    private num: Integer = 1;
    private den: Integer = 1;

    constructor(num: Integer, den: Integer) {
        this.num = num;
        this.den = den;
    }

    function reduce(fraction: Fraction: primes: Primes): Fraction {

}
    
    
}


namespace E66 {


    type Triple = {
        a: number,
        b: number,
        k: number,
    }
}